{
    IBClasses = (
        {
            ACTIONS = {bookmark = id; home = id; search = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addBookmarkButton = id; 
                backButton = id; 
                forwardButton = id; 
                homeButton = id; 
                searchProgressIndicator = id; 
                webView = id; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = ToolbarController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addBookmarkView = id; 
                fontSmallerAndBiggerView = id; 
                historyBackAndForwardView = id; 
                homeView = id; 
                searchView = id; 
                window = id; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}